source 'https://rubygems.org'

gemspec

# This should be grabbed from git, not rubygems, as we have our own fixes
gem 'xeroizer', :git => 'https://github.com/theodi/xeroizer.git'

# Using my branch for now, until it's pulled into Master
gem 'capsulecrm', :git => 'https://github.com/theodi/capsulecrm.git', :branch => 'adding-update-to-organisation' 